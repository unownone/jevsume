import StudioApp from "@/studio/StudioApp.tsx";

/** Studio owns the full viewport; site links live in studio chrome (no stacked marketing header). */
export default function ReviewStudioPage() {
  return <StudioApp />;
}
